apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-bad-app
  namespace: visualizer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bad-app
  template:
    metadata:
      labels:
        app: bad-app
    spec:
      containers:
      - name: nginx-container
        image: nginx