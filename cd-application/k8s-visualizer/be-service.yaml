apiVersion: v1
kind: Service
metadata:
  name: k8s-visualizer-be
  namespace: k8s-visualizer
spec:
  type: ClusterIP
  selector:
    app: k8s-visualizer-be
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
