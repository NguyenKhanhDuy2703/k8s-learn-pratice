apiVersion: v1
kind: Service
metadata:
  name: k8s-visualizer-be
  namespace: visualizer
spec:
  type: ClusterIP
  selector:
    app: k8s-visualizer-be
  ports:
    - name: http
      port: 3001
      targetPort: 3001
      protocol: TCP
